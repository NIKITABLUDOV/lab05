
Лабораторна робота №5. Циклічні конструкції.

Блудов Микита, КН-922Б 

Завдання: Визначити, чи є задане ціле число простим.

Код:
____________________________________________________________________
#include <stdio.h>

int main()
{
    int x, y, res;
    x = 20;
    y = 1;
    res = 0;
    
   while( x%y !=0 )
   {  
       y++;
   }
   if(x == y)
   {    
       res = 1;      
   }   
    return res;
}


Отримали:

-> 18       return 0;
   19   }
(lldb) p res
(int) $0 = 0
_____________________  

- для підтвердження коректності роботи програми, зупинено відлагодник на
 строці "y = 2;" та введемо команду "p x = 19". Після 
вводимо команду "c" і отримуємо наступне:

-> 18       return 0;
   19   }
(lldb) p res
(int) $2 = 1
___________________________________
#include <stdio.h>

int main() 
{
  int x = 20, y; 
  res = 0;  
  
   for(y = 2; x%y != 0; y++)
   {  
        
   }
   if( x == y && res == 0)
        {
            res = 1;
        }
   
   return 0;
}

Отримали:

-> 21       return 0;
   22   }
(lldb) p res
(int) $0 = 0

____________________________________________________________________ 

- для підтвердження коректності роботи програми, зупинено відлагодник на
 строці "res = 0;" та введемо команду "p x = 19". Після 
вводимо команду "c" і отримуємо наступне:

-> 16      return 0;
   17   }
(lldb) p res
(int) $2 = 1
____________________________________________________________________
#include <stdio.h>

int main()
{

int x, y, res;
x = 20;
y = 1;
res = 0;

   do
   {  
        y++;
        
   }
   while( x%y !=0);

   if(x == y)
   {
       res = 1;
   }

    return res;
}


Отримали:

-> 21       return 0;
   22   }
(lldb) p res
(int) $0 = 0
_____________________  

- для підтвердження коректності роботи програми, зупинено відлагодник на
 строці "y = 2;" та введемо команду "p x = 19". Після 
вводимо команду "c" і отримуємо наступне:

-> 21       return 0;
   22   }
(lldb) p res
(int) $2 = 1

______________________

Структура проекту лабораторної роботи:

.
└── lab05
    ├── Makefile
    ├── README.md
    ├── doc
    │   └── lab05.txt
    └── src
        └── main.c

Висновки: при виконанні лабораторної роботи були набуті практичні навички створення програм, що розгалужуються на мові С, 
зокрема: завдання умов, написання дій, якщо умова виконується і навпакию множення, та зміна результатів роботи програми на льоту. 